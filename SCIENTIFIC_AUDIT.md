# PLO Polar Studio 科研数据可信度审查与优化记录

审查日期：2026-08-17。范围：Chrome MV3 扩展入口、导入/解析、背景处理、三组切片、经验谐波拟合、可视化、CSV 与报告。

## 数据与模型的实际含义

- 输入是二维 `x, intensity` 序列；角度由 `x × multiplier` 计算。应用不保存原始文件，也不掌握仪器曝光、暗场、检偏器零位或样品元数据。
- 背景模块产生基线和净强度。若启用“截断负值”，显示净强度不再等同于代数相减结果。
- `MalusFitter` 是五项经验谐波最小二乘模型 `A0 + A4 cos(4θ) + B4 sin(4θ) + A2 cos(2θ) + B2 sin(2θ)`。它可量化曲线一致性，但不能仅凭一次强度扫描独立标定波片延迟、完整 Stokes 矢量或仪器精度。

## 原始审查发现

| 严重度 | 发现 | 对科研结论的影响 | 已采取措施 |
|---|---|---|---|
| 高 | 解析器静默跳过异常行，未报告重复 x、乱序、覆盖不足。 | 可能在用户不知情时改变有效样本。 | 增加自动质量门，报告有效点、覆盖、重复与顺序异常。 |
| 高 | 负净值被截断后仍用于 DoLP/ER。 | 零值可使消光比和 DoLP 偏高。 | 保留未截断列，导出两种净值；界面与报告显示警示。 |
| 高 | 自动相位锁定改变组间相位，却未界定用途。 | 会掩盖机械零位或重复性误差。 | 明确为“仅可视化对齐”，启用时结论降为限条件解释。 |
| 高 | 报告将经验谐波量写成波片延迟反演，并使用“完全契合”。 | 存在不可识别参数和过度因果解释。 | 改为模型依赖诊断量，报告完整限制与所需独立校准。 |
| 中 | `min(y)` 被称为暗电流。 | 统计极小值不等于暗场测量。 | 更名为显示基准，并标注限制。 |
| 中 | airPLS 被绝对化宣传。 | 容易诱导不做基线敏感性分析。 | 改为参数敏感性验证提示。 |
| 中 | 报告与导出缺少完整处理记录。 | 他人无法严格复现。 | 增加 UTC 时间、算法、参数、截断、相位处理、质量状态和警示。 |
| 低 | `popup/` 使用的 storage 键与主程序当前写入键不一致，且 manifest 未配置 popup。 | 该遗留入口目前不构成主流程。 | 建议在后续版本删除它，或重新接入并写 browser-level 测试。 |

## 已实现的可靠性护栏

1. `js/core/data-quality.js` 只审查、不改写输入；以 `supported / qualified / blocked` 表示可主张程度。
2. 质量卡片同时显示采样点数、角度覆盖、重复 x、负值截断和警示原因。
3. CSV 增加 `Raw_Y`、`Baseline_Y`、`Unclamped_Subtracted_Y`、`Displayed_Subtracted_Y`，并在文件头写出完整配置。
4. HTML 报告有独立“数据质量、处理可追溯性与结论边界”段落；不再将经验拟合表述为独立物理标定。

## 仍需实验侧提供的证据

- 同一曝光/增益下的暗场和空白；以独立暗场代替 `min(y)`。
- 角度零位、检偏器方位与输入偏振的校准记录。
- 至少三次完整独立扫描；不使用自动相位锁定时报告相位散布。
- 基线算法、参数、是否截断三维敏感性表；仅在主要结论稳定时报告处理后 DoLP/ER。
- 若要报告延迟量或完整 Stokes 参数，采用已知输入态和经校准的 Mueller/Jones 测量方案，并给出不确定度传播。

## 验证证据

- 已运行 JavaScript 语法检查：`data-quality.js`、`app.js`、`baseline-engine.js`、`report-engine.js`、`malus-fitter.js`。
- 已运行确定性回归：常数基线 `[10,9,11,8,12]` 扣除 10 后，未截断结果为 `[0,-1,1,-2,2]`，显示结果为 `[0,0,1,0,2]`，准确记录 2 个截断点；含重复 x、短组和未完成拟合的案例被标为 `blocked`。
- 临时本地 HTTP 服务返回 `200`。浏览器自动化未能执行：环境中的 npm cache 对 `@playwright/cli` 报 `EPERM`，与应用脚本无关；建议在可写 npm cache 的环境复跑 UI 操作测试。
